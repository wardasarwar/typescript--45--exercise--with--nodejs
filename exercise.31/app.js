var users = ['sana', 'saba', 'taha', 'baber', 'admin'];
if (users.length === 0) {
    console.log("we need to find some users");
}
else {
    users = [];
    console.log("All users have been remove" + users.length);
}
